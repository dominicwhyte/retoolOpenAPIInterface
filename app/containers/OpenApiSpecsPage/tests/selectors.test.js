// import { selectOpenApiSpecsPageDomain } from '../selectors';

describe('selectOpenApiSpecsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
