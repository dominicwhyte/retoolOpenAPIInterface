// import { selectInterfacePageDomain } from '../selectors';

describe('selectInterfacePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
