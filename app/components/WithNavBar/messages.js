/*
 * WithNavBar Messages
 *
 * This contains all the text for the WithNavBar component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.WithNavBar';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WithNavBar component!',
  },
});
