/*
 * TagEndpointsGroup Messages
 *
 * This contains all the text for the TagEndpointsGroup component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TagEndpointsGroup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TagEndpointsGroup component!',
  },
});
